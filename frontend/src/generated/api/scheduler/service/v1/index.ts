// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type RestoreMode =
  | "RESTORE_MODE_SKIP"
  | "RESTORE_MODE_OVERWRITE";
export type ExportBackupRequest = {
  tenantId?: number;
  includeSecrets: boolean | undefined;
};

export type ExportBackupResponse = {
  data: string | undefined;
  module: string | undefined;
  version: string | undefined;
  exportedAt: wellKnownTimestamp | undefined;
  tenantId: number | undefined;
  entityCounts: { [key: string]: number } | undefined;
  schemaVersion: number | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type ImportBackupRequest = {
  data: string | undefined;
  mode: RestoreMode | undefined;
};

export type ImportBackupResponse = {
  success: boolean | undefined;
  results: EntityImportResult[] | undefined;
  warnings: string[] | undefined;
  sourceVersion: number | undefined;
  targetVersion: number | undefined;
  migrationsApplied: number | undefined;
};

export type EntityImportResult = {
  entityType: string | undefined;
  total: number | undefined;
  created: number | undefined;
  updated: number | undefined;
  skipped: number | undefined;
  failed: number | undefined;
};

export interface BackupService {
  ExportBackup(request: ExportBackupRequest): Promise<ExportBackupResponse>;
  ImportBackup(request: ImportBackupRequest): Promise<ImportBackupResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createBackupServiceClient(
  handler: RequestHandler
): BackupService {
  return {
    ExportBackup(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/backup/export`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.tenantId) {
        queryParams.push(`tenantId=${encodeURIComponent(request.tenantId.toString())}`)
      }
      if (request.includeSecrets) {
        queryParams.push(`includeSecrets=${encodeURIComponent(request.includeSecrets.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "BackupService",
        method: "ExportBackup",
      }) as Promise<ExportBackupResponse>;
    },
    ImportBackup(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/backup/import`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "BackupService",
        method: "ImportBackup",
      }) as Promise<ImportBackupResponse>;
    },
  };
}
// SchedulerErrorReason defines error codes for the scheduler service
export type SchedulerErrorReason =
  | "BAD_REQUEST"
  | "UNAUTHORIZED"
  | "FORBIDDEN"
  | "NOT_FOUND"
  | "CONFLICT"
  | "INTERNAL_SERVER_ERROR"
  | "SERVICE_UNAVAILABLE";
// Task Execution record
export type TaskExecution = {
  executionId: string | undefined;
  taskType: string | undefined;
  moduleId: string | undefined;
  status: string | undefined;
  message?: string;
  attempt: number | undefined;
  durationMs: number | undefined;
  startedAt: wellKnownTimestamp | undefined;
  finishedAt?: wellKnownTimestamp;
};

// List Task Executions - Request
export type ListTaskExecutionsRequest = {
  taskType: string | undefined;
  page?: number;
  pageSize?: number;
};

// List Task Executions - Response
export type ListTaskExecutionsResponse = {
  items: TaskExecution[] | undefined;
  total: number | undefined;
};

// Task Options
export type TaskOption = {
  //
  // Behaviors: OPTIONAL
  maxRetry?: number;
  //
  // Behaviors: OPTIONAL
  timeout?: wellKnownDuration;
  //
  // Behaviors: OPTIONAL
  deadline?: wellKnownTimestamp;
  //
  // Behaviors: OPTIONAL
  processIn?: wellKnownDuration;
  //
  // Behaviors: OPTIONAL
  processAt?: wellKnownTimestamp;
  //
  // Behaviors: OPTIONAL
  uniqueTTL?: wellKnownDuration;
  //
  // Behaviors: OPTIONAL
  retention?: wellKnownDuration;
  //
  // Behaviors: OPTIONAL
  group?: string;
  //
  // Behaviors: OPTIONAL
  taskID?: string;
};

// Generated output always contains 0, 3, 6, or 9 fractional digits,
// depending on required precision, followed by the suffix "s".
// Accepted are any fractional digits (also none) as long as they fit
// into nano-seconds precision and the suffix "s" is required.
type wellKnownDuration = string;

// Scheduled Task
export type Task = {
  id?: number;
  type?: Task_Type;
  typeName?: string;
  taskPayload?: string;
  cronSpec?: string;
  taskOptions?: TaskOption;
  // Module ID that owns this task type (set automatically from task type registry).
  moduleId?: string;
  enable?: boolean;
  remark?: string;
  // Execution tracking (set by scheduler after each run)
  lastRunAt?: wellKnownTimestamp;
  lastRunStatus?: string;
  lastRunMessage?: string;
  runCount?: number;
  tenantId?: number;
  createdBy?: number;
  updatedBy?: number;
  deletedBy?: number;
  createdAt?: wellKnownTimestamp;
  updatedAt?: wellKnownTimestamp;
  deletedAt?: wellKnownTimestamp;
};

// Scheduled Task Type
export type Task_Type =
  | "PERIODIC"
  | "DELAY"
  | "WAIT_RESULT";
// List Tasks - Request
export type ListTasksRequest = {
  page?: number;
  pageSize?: number;
  orderBy?: string;
  noPaging?: boolean;
  filter: { [key: string]: string } | undefined;
  fieldMask?: wellKnownFieldMask;
};

// In JSON, a field mask is encoded as a single string where paths are
// separated by a comma. Fields name in each path are converted
// to/from lower-camel naming conventions.
// As an example, consider the following message declarations:
//
//     message Profile {
//       User user = 1;
//       Photo photo = 2;
//     }
//     message User {
//       string display_name = 1;
//       string address = 2;
//     }
//
// In proto a field mask for `Profile` may look as such:
//
//     mask {
//       paths: "user.display_name"
//       paths: "photo"
//     }
//
// In JSON, the same mask is represented as below:
//
//     {
//       mask: "user.displayName,photo"
//     }
type wellKnownFieldMask = string;

// List Tasks - Response
export type ListTasksResponse = {
  items: Task[] | undefined;
  total: number | undefined;
};

// Get Task - Request
export type GetTaskRequest = {
  id?: number;
  typeName?: string;
  viewMask?: wellKnownFieldMask;
};

// Create Task - Request
export type CreateTaskRequest = {
  data: Task | undefined;
};

// Update Task - Request
export type UpdateTaskRequest = {
  id: number | undefined;
  data: Task | undefined;
  updateMask: wellKnownFieldMask | undefined;
  allowMissing?: boolean;
};

// Delete Task - Request
export type DeleteTaskRequest = {
  id: number | undefined;
};

// Restart All Tasks - Response
export type RestartAllTasksResponse = {
  count: number | undefined;
};

// Control Task - Request
export type ControlTaskRequest = {
  controlType: ControlTaskRequest_ControlType | undefined;
  typeName: string | undefined;
};

export type ControlTaskRequest_ControlType =
  | "START"
  | "STOP"
  | "RESTART";
// List Task Type Names - Response
export type ListTaskTypeNamesResponse = {
  typeNames: string[] | undefined;
};

// Scheduled Task Management Service
export interface SchedulerTaskService {
  // Query scheduled task list
  ListTasks(request: ListTasksRequest): Promise<ListTasksResponse>;
  // Query scheduled task details
  GetTask(request: GetTaskRequest): Promise<Task>;
  // Create scheduled task
  CreateTask(request: CreateTaskRequest): Promise<Task>;
  // Update scheduled task
  UpdateTask(request: UpdateTaskRequest): Promise<Task>;
  // Delete scheduled task
  DeleteTask(request: DeleteTaskRequest): Promise<wellKnownEmpty>;
  // List registered task type names
  ListTaskTypeNames(request: wellKnownEmpty): Promise<ListTaskTypeNamesResponse>;
  // Restart all scheduled tasks
  RestartAllTasks(request: wellKnownEmpty): Promise<RestartAllTasksResponse>;
  // Start all scheduled tasks
  StartAllTasks(request: wellKnownEmpty): Promise<wellKnownEmpty>;
  // Stop all scheduled tasks
  StopAllTasks(request: wellKnownEmpty): Promise<wellKnownEmpty>;
  // Control a specific scheduled task (start/stop/restart)
  ControlTask(request: ControlTaskRequest): Promise<wellKnownEmpty>;
  // List execution history for a task
  ListTaskExecutions(request: ListTaskExecutionsRequest): Promise<ListTaskExecutionsResponse>;
}

export function createSchedulerTaskServiceClient(
  handler: RequestHandler
): SchedulerTaskService {
  return {
    ListTasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.orderBy) {
        queryParams.push(`orderBy=${encodeURIComponent(request.orderBy.toString())}`)
      }
      if (request.noPaging) {
        queryParams.push(`noPaging=${encodeURIComponent(request.noPaging.toString())}`)
      }
      if (request.filter) {
        request.filter.forEach((value, key) => {
          queryParams.push(`filter[key]=${encodeURIComponent(value.toString())}`)
        })
      }
      if (request.fieldMask) {
        queryParams.push(`fieldMask=${encodeURIComponent(request.fieldMask.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "ListTasks",
      }) as Promise<ListTasksResponse>;
    },
    GetTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tasks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.typeName) {
        queryParams.push(`typeName=${encodeURIComponent(request.typeName.toString())}`)
      }
      if (request.viewMask) {
        queryParams.push(`viewMask=${encodeURIComponent(request.viewMask.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "GetTask",
      }) as Promise<Task>;
    },
    CreateTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "CreateTask",
      }) as Promise<Task>;
    },
    UpdateTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tasks/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "UpdateTask",
      }) as Promise<Task>;
    },
    DeleteTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tasks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "DeleteTask",
      }) as Promise<wellKnownEmpty>;
    },
    ListTaskTypeNames(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks:type-names`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "ListTaskTypeNames",
      }) as Promise<ListTaskTypeNamesResponse>;
    },
    RestartAllTasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks:restart`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "RestartAllTasks",
      }) as Promise<RestartAllTasksResponse>;
    },
    StartAllTasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks:start`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "StartAllTasks",
      }) as Promise<wellKnownEmpty>;
    },
    StopAllTasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks:stop`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "StopAllTasks",
      }) as Promise<wellKnownEmpty>;
    },
    ControlTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tasks:control`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "ControlTask",
      }) as Promise<wellKnownEmpty>;
    },
    ListTaskExecutions(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/task-executions`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.taskType) {
        queryParams.push(`taskType=${encodeURIComponent(request.taskType.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SchedulerTaskService",
        method: "ListTaskExecutions",
      }) as Promise<ListTaskExecutionsResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// TaskTypeDescriptor describes a single executable task type.
export type TaskTypeDescriptor = {
  // Globally unique task type name. Convention: "{module_id}:{action}"
  taskType: string | undefined;
  // Human-readable display name for the UI.
  displayName: string | undefined;
  // Description of what this task does.
  description: string | undefined;
  // JSON Schema for the payload (optional, used for UI form generation).
  payloadSchema: string | undefined;
  // Default cron expression suggestion.
  defaultCron: string | undefined;
  // Default max retries.
  defaultMaxRetry: number | undefined;
};

// RegisterTaskTypesRequest is sent by a module to declare its task types.
export type RegisterTaskTypesRequest = {
  // Module ID of the calling module (e.g., "paperless", "deployer").
  moduleId: string | undefined;
  // Task types this module can execute.
  taskTypes: TaskTypeDescriptor[] | undefined;
};

export type RegisterTaskTypesResponse = {
  registeredCount: number | undefined;
  message: string | undefined;
};

export type UnregisterTaskTypesRequest = {
  moduleId: string | undefined;
};

export type ListRegisteredTaskTypesResponse = {
  taskTypes: RegisteredTaskType[] | undefined;
};

// RegisteredTaskType is a task type with its owning module info.
export type RegisteredTaskType = {
  taskType: string | undefined;
  moduleId: string | undefined;
  displayName: string | undefined;
  description: string | undefined;
  payloadSchema: string | undefined;
  defaultCron: string | undefined;
  defaultMaxRetry: number | undefined;
};

// TaskTypeRegistrationService allows modules to register their executable
// task types with the scheduler. Modules call this on startup to declare
// which task types they can handle. The scheduler then calls back to the
// module's TaskExecutorService when a task of that type fires.
export interface TaskTypeRegistrationService {
  // RegisterTaskTypes declares which task types a module can execute.
  // Idempotent — re-registration overwrites previous entries for the same module.
  RegisterTaskTypes(request: RegisterTaskTypesRequest): Promise<RegisterTaskTypesResponse>;
  // UnregisterTaskTypes removes all task types for a module (called on shutdown).
  UnregisterTaskTypes(request: UnregisterTaskTypesRequest): Promise<wellKnownEmpty>;
  // ListRegisteredTaskTypes returns all registered task types across all modules.
  ListRegisteredTaskTypes(request: wellKnownEmpty): Promise<ListRegisteredTaskTypesResponse>;
}

export function createTaskTypeRegistrationServiceClient(
  handler: RequestHandler
): TaskTypeRegistrationService {
  return {
    RegisterTaskTypes(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/task-types:register`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TaskTypeRegistrationService",
        method: "RegisterTaskTypes",
      }) as Promise<RegisterTaskTypesResponse>;
    },
    UnregisterTaskTypes(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/task-types:unregister`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TaskTypeRegistrationService",
        method: "UnregisterTaskTypes",
      }) as Promise<wellKnownEmpty>;
    },
    ListRegisteredTaskTypes(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/task-types`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TaskTypeRegistrationService",
        method: "ListRegisteredTaskTypes",
      }) as Promise<ListRegisteredTaskTypesResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
